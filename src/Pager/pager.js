import '../../style/pager.scss'

// {
  class Pager {
    constructor ($container) {
      this.$container = $container
      console.log('pager')
    }
  }
  // window.Pager = Pager
// }
export default Pager
